machine:
  php:
    version: 5.6.5
dependencies:
  post:
    - cp ~/reecms/.env.example ~/reecms/.env
    - php ~/reecms/artisan key:generate
    
deployment:
  dev:
    branch: master
    heroku:
      appname: infinite-dawn-1227
